PORT=3000

DATABASE_URL="postgresql://thefresh:thefresh@localhost:5434/thefresh?schema=public"

REDIS_HOST=localhost
REDIS_PORT=6381

JWT_SECRET=change-me-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Firebase Admin — verifies ID tokens from client-side Firebase Phone Auth (Flutter app).
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
